const SearchMobile = ({mobileSearchShow, onClick}) => {
    return (
        <div className={`mobile-search-container ${mobileSearchShow}`}>
            <button onClick={onClick}>close</button>
        <form action="">
        
        </form>
        </div>
    )
}

export default SearchMobile;